(function () {
  'use strict';

  /**
   * @ngdoc overview
   * @name <%= _.camelCase(moduleName) %>
   * @id angularApp.common.<%= _.camelCase(moduleName) %>
   */
  angular.module('angularApp.common.<%= _.camelCase(moduleName) %>', [
    // Dependencies here
  ]);
})();
