(function () {
  'use strict';

  /**
   * @ngdoc overview
   * @name search
   * @id angularApp.components.<%= _.camelCase(moduleName) %>
   */
  angular.module('angularApp.components.<%= _.camelCase(moduleName) %>', [
    // Dependencies here
  ]);
})();
